@model IEnumerable<GCRS.Domain.UnidadTiempoRenta>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<h2>UnidadesTiempoRentas</h2>
@Html.ActionLink("Add", "AddUnidadTiempoRenta")
<br />
<table class="table">
    @*Listar UnidadesTiempoRenta*@
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.Encode(item.Nombre)
            </td>
            <td>
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
